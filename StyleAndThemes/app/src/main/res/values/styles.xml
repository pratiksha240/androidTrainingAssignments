<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="android:gravity">center_horizontal</item>
        <item name="switchStyle">@style/SwitchLightMode</item>
        <item name="imageviewStyle">@style/ImageviewLightMode</item>
        <item name="textviewStyle">@style/TextviewLightMode</item>
    </style>

    <style name="MyThem"  parent="AppTheme">
        <item name="android:gravity">center_horizontal</item>
        <item name="android:background">#040001</item>
        <item name="height">300dp</item>
        <item name="switchStyle">@style/SwitchDarkMode</item>
        <item name="imageviewStyle">@style/ImageviewDarkMode</item>
        <item name="textviewStyle">@style/TextviewDardMode</item>
    </style>

</resources>
